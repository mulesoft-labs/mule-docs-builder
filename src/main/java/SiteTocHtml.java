import java.util.List;

/**
 * Created by sean.osterberg on 2/20/15.
 */
public class SiteTocHtml {
    private String html;

    /*
    public static String fromSectionsAndPage(Section currentSection, List<Section> allSections, Page page) {
        StringBuilder builder = new StringBuilder();
        for(Section section : allSections) {
            if(section.getUrl().contentEquals(currentSection.getUrl())) {

            } else {

            }
        }
    }*/
}
